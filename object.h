#ifndef OBJECT_H_
#define OBJECT_H_

// Holding information as the old system
struct Good;

// Having a char and a int for representing a shelf
struct Shelf;

// Node with 2 Node pointers and 1 Good pointer
struct Node;

// Linked list with action that holds information to undo all previous actions
struct Action; 

// The shopingcart where you can hold wares and information about them
struct ShoppingCart;

#endif
